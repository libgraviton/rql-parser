language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm
  - hhvm-nightly

before_script:
  - composer self-update
  - composer install --dev

script:
  ./vendor/bin/phpunit -v

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm-nightly
